# 📂 Binary-Data-Processor-Python-C

## 💡 Обзор и концепция проекта

Этот проект демонстрирует полный цикл обработки данных, фокусируясь на **межъязыковом взаимодействии** через бинарный файл

**Цель:** обеспечить строгую передачу структурированных данных о студентах от скрипта-генератора на **Python** к программе-сортировщику на **C**, и затем обратно к скрипту-анализатору на **Python** для финального форматирования

### 🛠️ Использованные технологии и подходы

* **Языки:** Python, C
* **Синхронизация данных:** использован "контракт данных" с модулем **`struct`** (Python) и директивой **`#pragma pack(1)`** (C) для обеспечения точной совместимости структур
* **Сортировка:** **сортировка "пузырьком"** реализована на C, работающая **напрямую с файлом** через `fseek`, `fread` и `fwrite`
* **Архитектура Python:** применен объектно-ориентированный подход с классами **`Student`** и **`BinProcessor`** для чистой инкапсуляции и модульности кода

---

## 💾 Структура бинарной записи (`data.bin`)

Основой проекта является строгий формат данных. Каждая запись имеет фиксированный размер **36 байт** и использует формат **Little-endian** (`<`) для кросс-платформенной совместимости

### 🔍 Детали формата `struct`: `<Id16s8s`

Строка `<Id16s8s` - это **форматный код** для модуля `struct` в Python, определяющий точный порядок, размер и тип каждого элемента данных:

| Поле | Формат Python | Тип C | Размер (байты) | Описание                                                            |
| :--- | :--- | :--- | :---: |:--------------------------------------------------------------------|
| **Порядок** | **`<`** | **Little-endian** | — | Указывает на прямой порядок байтов (младший значащий байт в начале) |
| ID | `I` | `unsigned int` | 4 | Идентификатор студента                                              |
| GPA | `d` | `double` | 8 | Средний балл                                                        |
| Фамилия | `16s` | `char[16]` | 16 | Строка фиксированной длины                                          |
| Группа | `8s` | `char[8]` | 8 | Строка фиксированной длины                                          |

---

## 📝 Этапы реализации проекта

### 1. Генерация исходных данных (`pybin_start.py`)

* **Назначение:** создает неотсортированный файл **`data.bin`**
* **Техника:** использует функцию **`struct.pack()`** для записи структур Python. Строковые поля кодируются с нулевыми байтами, чтобы соответствовать C-структуре

### 2. Сортировка данных на C (`binarnik.c`)

* **Назначение:** принимает `data.bin` и выполняет сортировку по полю **ID**, обновляя файл на месте
* **Техника:**
    * Использована директива **`#pragma pack(1)`**, чтобы гарантировать, что размер C-структуры точно равен 36 байтам
    * Применяется сложный механизм прямого доступа к файлу: функции **`fseek()`** и **`swap_structures()`** (с использованием `fread/fwrite`) позволяют изменять порядок записей без загрузки всего файла в оперативную память

### 3. Финальное чтение и вывод (`pybin_end_oop.py`)

* **Назначение:** Читает отсортированный **`data.bin`** и генерирует отчет **`output.txt`**
* **Техника:**
    * **`struct.unpack()`** используется для чтения и декодирования байтов
    * Применяется **`.decode('utf-8').strip('\x00')`** для корректного удаления нулевых байтов-заполнителей из строк
    * Финальный вывод записывается в **`output.txt`** с использованием метода **`__str__`** класса `Student`, что обеспечивает аккуратное табличное форматирование